function makePlots(data_structure)

% Max intinsty of event
event_max_intensity (data_structure.event_max_intensity);

% Cumulative release duration
cum_release_duration(data_structure.cum_release_duration(:,2));   

% Percent of each type of release
percent_release_type(data_structure.release_type_calc)

% Relative Content Release
cum_content_release(data_structure.cum_event_intensity_decrease(:,2))